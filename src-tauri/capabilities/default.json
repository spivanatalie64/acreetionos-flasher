{
  "identifier": "default",
  "description": "Default capability granting IPC access to the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default"
  ]
}
